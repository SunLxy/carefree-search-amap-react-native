{
  "name": "root",
  "private": true,
  "workspaces": {
    "packages": [
      "core",
      "examples",
      "shared"
    ],
    "nohoist": [
      "example/**"
    ]
  },
  "scripts": {
    "version": "lerna version --exact --force-publish --no-push --no-git-tag-version"
  },
  "devDependencies": {
    "lerna": "^7.0.1"
  }
}